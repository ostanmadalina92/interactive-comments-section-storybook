import React from "react";

import DeleteModalReply from "./DeleteModalReply";

export default {
  component: DeleteModalReply,
  title: "DeleteModalReply",
};

const Template = (args) => <DeleteModalReply {...args} />;

export const Default = Template.bind({});
Default.args = {};
